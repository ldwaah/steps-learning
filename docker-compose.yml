services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: steps
      POSTGRES_PASSWORD: steps_dev
      POSTGRES_DB: steps
    ports:
      - "5432:5432"
    volumes:
      - steps_pg_data:/var/lib/postgresql/data

volumes:
  steps_pg_data:
